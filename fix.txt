Fix incremental clip saving & part timing
This plan will diagnose and fix why clips don’t appear in the Clips view as soon as each part finishes, and adjust the recording/timing logic so multi-part recordings behave predictably (including potential overlap between parts).

1. Understand and reproduce the current behavior
Instrument the pipeline (read-only first, then temporary logs)
Trace the sequence: uploadUI.startProcessing → clipGenerator.processUpload → generateClips → videoProcessor.extractClipsBatch → _saveClip → window.dispatchEvent('clip:saved') → clipsUI.refresh.
Reproduce with a short test video in both normal mode and "Series" mode, watching:
When the modal status changes from "Recording clip 1" → "Finalizing" → "Recording clip 2".
Exactly when clip:saved fires for each part.
Whether clipsUI.refresh is being called and whether db.getAll(STORES.CLIPS) contains the first clip while the second is already being recorded.
Confirm content vs. UI issues
Verify if the first clip file truly exists in IndexedDB and is playable (even if not visible yet in the UI).
Check if clip start/end times and durations match the expected part boundaries.
2. Fix incremental saving & Clips view synchronization
Ensure per-clip persistence is atomic and event-driven
Double-check _saveClip always awaits both videoStore.saveBlob and db.put(STORES.CLIPS, record) before firing clip:saved.
Add robust error handling/logging so we can see if the first clip ever fails to save or throws silently.
Align UI updates with events
Verify window.addEventListener('clip:saved', ...) in clipsUI is always registered (module loaded) before generation starts.
Make sure clipsUI.refresh() correctly updates the internal clipsUI.clips array and re-renders while generation is ongoing.
If necessary, gate the refresh to only run when the Clips view is active, but still guarantee that by the time the UI shows "Recording Part 2", Part 1 is already visible in Clips.
3. Clarify and, if desired, implement part-overlap behavior
Clarify requirements for overlap
Decide on a concrete rule like: in series mode, Part N+1 starts overlapSeconds (e.g. 2–3s) before the end of Part N.
Confirm whether this should apply only in seriesMode or also to the score-based auto clips.
Adjust candidate generation for series mode
In ClipGenerator.processUpload, when building sequential candidates for seriesMode, modify start times to include the desired overlap while still respecting total duration.
Ensure overlays (Part N labels) and metadata (titles, part numbers) stay consistent with the new timings.
Verify interaction with MediaRecorder timing
Review videoProcessor._extractClipWithAudioMix / _extractClipMediaRecorder stop timers and buffers (including the extra 300ms tail) to ensure they don’t cause gaps or unexpected drift between parts.
4. End-to-end testing scenarios
Manual QA flows
Short video producing 2–4 parts, with and without BGM, in both normal and series modes.
While the modal shows "Recording clip 2", confirm that Clip 1 already appears in the Clips grid and is playable.
If overlap is enabled, verify that Part 2 starts a few seconds before Part 1 ends and that there are no missing moments.
Edge cases
Very short source video (only 1 or 2 clips possible).
User presses "Run in background" or closes the processing modal mid-run and then navigates to Clips to ensure incremental visibility still works.
Once you’re happy with this plan, I’ll switch out of planning mode and start implementing: first with instrumentation to confirm the exact bug, then with targeted fixes to saving, UI sync, and (optionally) part overlap.